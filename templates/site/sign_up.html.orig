{% extends 'site/bootstrap.html' %}
{% load static %}
{% load i18n %}

{% block title %}{{ title }} - {{ site_title }}{% endblock %}

{% block head %}
    <link rel="stylesheet" href="{% static 'bootstrap/sign_up.css' %}" />
{% endblock %}

{% block content %}
    <section>
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    {% trans 'Join the future in retail business.' %}
                    <p class="free-ad">
                        {% trans 'Register for free.' %}
                    </p>
                </div>
<<<<<<< HEAD
                <form action="" >
                    <div class="col-md-4">
                        <div class="form-field">
                            {{ user_form.first_name.label_tag }}
                            {{ user_form.first_name}}
                        </div>

                        <div class="form-field">
                            {{ user_form.last_name.label_tag }}
                            {{ user_form.last_name }}
                        </div>

                        <div class="form-field">
                            {{ user_form.email.label_tag }}
                            {{ user_form.email }}
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-field">
                            {{ user_form.password1.label_tag }}
                            {{ user_form.password1 }}
                        </div>
                        <div class="form-field">
                            {{ user_form.password2.label_tag }}
                            {{ user_form.password2 }}
                        </div>

                        <div class="form-field">
                            <input type="submit" class="buttons" value="{% trans 'Register' %}" />
                        </div>
                    </div>
                </form>
=======

                <div id="sign_up_form">
                    <form action="{% url 'sign_up' %}" method="post" class="registration">
                        <div class="register less {% if registration_successful %}hidden{% endif %}">
                            {% csrf_token %}

                            <p class="label">{% trans 'E-mail' %}</p>

                            <div class="second">{{ user_form.email }}
                                <span class="helptext">{% trans "This field is required." %}</span>
                                {% if user_form.email.errors %}
                                    <span class="email errors">
                                        {% for error in user_form.email.errors %}{{ error }}{% endfor %}
                                    </span>
                                {% endif %}

                                {% if message_code == 'user-exists-google' or message_code == 'user-exists-normal' %}
                                    <span id="user_exists_message" class="errors">
                                        {{ message }}

                                        <div id="blocklogic_websites_list">
                                            <a href="https://www.timebits.com" target="_blank">timebits.com</a><br/>
                                            <a href="https://www.sellout.biz" target="_blank">sellout.biz</a>
                                        </div>
                                    </span>
                                {% endif %}
                            </div>

                            <p class="label">{% trans 'First' %}</p>

                            <div class="second">
                                {{ user_form.first_name }}
                                <span class="helptext">
                                    {% trans "This field is required. 30 characters or less. Only letters and numbers allowed." %}
                                </span>

                                {% if user_form.first_name.errors %}<span class="errors">
                                    {% for error in user_form.first_name.errors %}{{ error }}{% endfor %}
                                    </span>{% endif %}
                            </div>

                            <p class="label">{% trans 'Last name' %}</p>

                            <div class="second">
                                {{ user_form.last_name }}

                                <span class="helptext">
                                    {% trans "This field is required. 30 characters or less. Only letters and numbers allowed." %}
                                </span>

                                {% if user_form.last_name.errors %}<span class="errors">
                                    {% for error in user_form.last_name.errors %}{{ error }} {% endfor %}
                                    </span>{% endif %}
                            </div>

                            <p class="label">{% trans 'Country' %}</p>

                            <div class="second">{{ user_form.country }}
                                <span class="helptext">{% trans "This field is required." %}</span>
                                {% if user_form.country.errors %}
                                    <span class="email errors">
                                        {% for error in user_form.country.errors %}{{ error }}{% endfor %}
                                    </span>
                                {% endif %}
                            </div>

                            <p class="label">{% trans 'Sex' %}</p>

                            <div class="second">
                                {{ user_form.sex }}
                                <span class="sex errors">{% if user_form.sex.errors %}
                                    {% for error in user_form.sex.errors %}
                                        {{ error }} {% endfor %}{% endif %}
                                </span>
                            </div>

                            <p class="label">{% trans 'Password' %}</p>

                            <div class="second">{{ user_form.password1 }} <span class="helptext">
                                {% trans "This field is required." %}</span>

                                {% if user_form.password1.errors %}
                                <span class="password1 errors">
                                    {% for error in user_form.password1.errors %}
                                    {{ error }} {% endfor %}</span>{% endif %}
                            </div>

                            <p class="label">{% trans 'Password one more time' %}</p>

                            <div class="second">{{ user_form.password2 }} <span
                                    class="helptext">{% trans "Password should be the same as in the previous field." %}</span>
                                {% if user_form.password2.errors %}
                                    <span class="errors">{% for error in user_form.password2.errors %}
                                        {{ error }}{% endfor %}
                                            </span>{% endif %}
                            </div>

                            <div class="second captcha">
                                {% if user_form.non_field_errors %}<span class="captcha-error">
                                    {% for error in user_form.non_field_errors %}{{ error }}{% endfor %}</span>
                                {% endif %}

                                <img id="captcha" src="{{ STATIC_URL }}images/site/captchas/{{ captcha_file }}" alt="captcha" />
                                <input type="text" id="captcha_user_answer" name="captcha_user_answer"/>
                            </div>
                        </div>

                        {% if message  %}
                            <div id="sign_up_message" class="less {% if registration_successful %}success{% endif %}">{{ message }}</div>
                        {% endif %}

                        {% if registration_successful %}
                            <div class="lost-login">
                                <a href="{% url 'index' %}" class="back">{% trans "Back" %}</a>
                            </div>
                        {% else %}
                            <div id="submit_container">
                                <input type="submit"
                                       id="submit"
                                       name="submit"
                                       value="{% trans "Sign up" %}"
                                       class="buttons hoverable"/>
                            </div>
                        {% endif %}
                    </form>
                </div>
>>>>>>> f361ad50e7fac6bfa562124d184509bc4f625269
            </div>
        </div>
    </section>
{% endblock %}